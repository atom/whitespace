{
  "name": "whitespace",
  "version": "0.10.0",
  "main": "./lib/whitespace",
  "description": "Strip trailing whitespace and add a trailing newline when the current editor is saved.",
  "repository": "https://github.com/atom/whitespace",
  "engines": {
    "atom": "*"
  },
  "devDependencies": {
    "temp": "~0.6.0"
  }
}
